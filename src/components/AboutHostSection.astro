<section class="about-host light-nav-zone" aria-label="Host and initiator">
	<div class="about-host-frame">
		<div class="host-row">
			<div class="host-mark">
				<p>Host &amp; initiator</p>
				<a href="https://500.co" aria-label="Visit 500 Global">
					<img src="/500_Global_Logo.svg" alt="500 Global" width="140" height="48" />
				</a>
			</div>

			<p class="host-copy">
				<a href="https://500.co">500 Global</a> is a venture capital firm with more than
				$2.1B in assets under management. Since 2010, it has backed founders across 80+
				countries and invested early in companies including Grab, Carsome, Carousell,
				Canva and Udemy. Its portfolio includes 3,000+ companies, 35+ unicorns and 270+
				exits.
			</p>
		</div>
	</div>
</section>

<style>
	.about-host {
		padding: clamp(1.25rem, 2.5vw, 2rem) clamp(0.75rem, 1.6vw, 1.5rem);
		color: var(--text-bright);
		background: var(--page-background);
	}

	.about-host-frame {
		display: grid;
		grid-template-columns: minmax(14rem, 18rem) minmax(0, 34rem);
		justify-content: center;
		align-items: center;
		gap: clamp(2.25rem, 5vw, 4.5rem);
		width: min(62rem, 100%);
		margin-inline: auto;
		padding: clamp(1.25rem, 2.2vw, 1.75rem) clamp(1.25rem, 3vw, 2rem);
	}

	.host-mark > p {
		margin: 0;
		color: var(--text-accent);
		font-size: 0.7rem;
		letter-spacing: 0.16em;
		line-height: 1.4;
		text-transform: uppercase;
	}

	.host-row {
		display: contents;
	}

	.host-mark {
		display: grid;
		gap: 0.85rem;
	}

	.host-mark a {
		display: block;
		width: fit-content;
	}

	.host-mark img {
		display: block;
		width: clamp(8.5rem, 14vw, 12rem);
		height: auto;
		filter: grayscale(1) invert(1) brightness(1.4);
	}

	:global(html[data-theme='light']) .host-mark img {
		filter: none;
	}

	.host-copy {
		margin: 0;
		color: var(--text-soft);
		font-size: clamp(0.96rem, 1.2vw, 1.08rem);
		line-height: var(--leading-body);
	}

	.host-copy a {
		color: var(--link-bright);
		text-decoration-color: var(--hairline);
		text-underline-offset: 0.22rem;
	}

	.host-mark a:focus-visible,
	.host-copy a:focus-visible {
		outline: 2px solid var(--focus-color);
		outline-offset: 4px;
	}

	@media (max-width: 860px) {
		.about-host-frame {
			grid-template-columns: 1fr;
			justify-content: stretch;
			width: 100%;
			gap: 1.25rem;
		}
	}

	@media (max-width: 700px) {
		.about-host {
			padding: 1.5rem 0.65rem;
		}

		.about-host-frame {
			padding: 0.75rem 1rem 1rem;
		}
	}
</style>
